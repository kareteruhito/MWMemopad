<Window x:Class="MWMemopad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MWMemopad"
        mc:Ignorable="d"
        Title="メモ帳" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="新規" Click="New_Click"/>
                <MenuItem Header="開く" Click="Open_Click"/>
                <MenuItem Header="保存" Click="Save_Click"/>
                <Separator />
                <MenuItem Header="終了" Click="Exit_Click"/>
            </MenuItem>
        </Menu>

        <TextBox x:Name="Editor"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="16"/>
    </DockPanel>
</Window>
